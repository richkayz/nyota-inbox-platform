# Frontend (Vite / Cloudflare Workers) — see gateway/.env.example for the backend.

# mock (default, uses in-memory fixtures) | live (hits the NestJS gateway)
VITE_API_MODE=mock

# Only used when VITE_API_MODE=live. Point at your gateway behind the Plesk nginx.
VITE_API_BASE_URL=/api
